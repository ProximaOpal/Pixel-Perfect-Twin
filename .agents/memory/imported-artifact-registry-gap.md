# Local workspace note

This project was originally imported from a Replit monorepo. Replit-specific
config (`.replit`, `.replit-artifact`, `@replit/*` Vite plugins, connectors SDK)
has been removed. Run the app locally with:

```bash
pnpm install
pnpm db:push
pnpm dev
```

Open http://localhost:5173
